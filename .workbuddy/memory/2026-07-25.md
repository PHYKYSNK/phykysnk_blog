# 2026-07-25 管理面板 + 日志增删 + 删除文章

- scripts/new-post.js 重构：新增统一管理面板，无参数时默认进入菜单模式
- 菜单集成 5 个功能：创建新文章 / 注册已有文件 / 删除文章 / 添加日志 / 删除日志
- 新增 --delete 模式：浏览已注册文章，选择删除（多选），自动保存 changelog
- 新增 --changelog-add 模式：手动添加日志条目（类型/描述/关联文章）
- 新增 --changelog-delete 模式：浏览日志列表，选择删除
- changelog UI 新增 "删除" 类型标识（红色标签）
- main.js 新增 '删除' 到 badgeMap
- 保留 --scan / 快速模式入口
- 新增 --edit 模式：选择文章 → 自动用 VS Code/记事本打开 → 确认后更新 updatedAt + changelog + git push
- 菜单新增"编辑文章"选项（第 6 项）
- 创建的 blog.bat 快捷启动脚本
